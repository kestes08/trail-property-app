# Optional. Without it, the Home map uses Google's keyless interactive embed
# (pan/zoom + click through to Google Maps). Set a key to switch to the
# official Maps Embed API with a satellite view.
#
# Copy this file to `.env`, add your key, and restart the dev server.
# VITE_GOOGLE_MAPS_API_KEY=your_key_here
